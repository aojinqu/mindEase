<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.MindEaseApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MindEase">
        <activity
            android:name=".feature.agent.AgentChatActivity"
            android:exported="false" />
        <activity
            android:name=".feature.community.PostDetailActivity"
            android:exported="false" />
        <activity
            android:name=".feature.community.PostEditorActivity"
            android:exported="false" />
        <activity
            android:name=".feature.settings.SettingsActivity"
            android:exported="false" />
        <activity
            android:name=".feature.mood.MoodEditorActivity"
            android:exported="false" />
        <activity
            android:name=".feature.auth.AuthActivity"
            android:exported="false" />
        <activity
            android:name=".feature.onboarding.OnboardingActivity"
            android:exported="false" />
        <activity
            android:name=".feature.main.MainActivity"
            android:exported="false" />
        <activity
            android:name="com.mindease.feature.splash.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>

